var lox = "lox";
var count = 1;

fun turnAround(count, limit) {
  return count == limit;
}

fun increment(count, delta) {
   return count + delta;
}

fun makeFish(limit) {
   var goBack = false;
   while (count > 0) {

      var loxes = "";

      for (var i = 0; i < count; i = i + 1) {
         loxes = loxes + lox;
      }

      print(loxes);

      if (!goBack) {
         goBack = turnAround(count, limit);
         count = increment(count, 1);
      } else if (goBack) {
         count = increment(count, -1);
      }
   }
}

makeFish(5);