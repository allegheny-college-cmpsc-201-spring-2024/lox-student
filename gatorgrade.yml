setup: |
  echo "🧹\tClean from previous compilation (mvn clean)"
  mvn clean -q || true
  echo "🛠️\tCompiling program (mvn compile)"
  mvn compile -q || true
---
- description: The interpreter program executes successfully (mvn exec:java)
  category: Tests
  check: ExecuteCommand
  options:
    command: cd interpreter && mvn exec:java
- description: Test cases pass; program prints "Hello, World!" (mvn test)
  category: Tests
  check: ExecuteCommand
  options:
    command: mvn test
- interpreter/src/test/resources/hello.lox:
  - description: Program contains at least 3 Lox language comments
    category: Lox Programming
    check: CountSingleLineComments
    options:
      language: Java
      count: 3
      exact: false
